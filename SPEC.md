# مواصفات تطبيق «متابعة الصلاة» (Salah Tracker)

> هذا الملف مرجع كامل لإعادة بناء التطبيق من الصفر. أعطِه للمساعد وسيبني نفس التطبيق.

---

## 1. الفكرة والهدف

تطبيق ويب (PWA) عربي لتتبّع الصلوات الخمس اليومية بأسلوب تحفيزي (Gamification): المستخدم يسجّل «مستوى» كل صلاة (أدركها قبل الإقامة؟ في المسجد؟ فاتته؟)، ويحصل على نقاط ورُتب وسلاسل (Streaks) وإنجازات، مع تحليلات ذكية لأدائه، وأوقات صلاة محسوبة محلياً، وأذكار ما بعد الصلاة.

- **اللغة:** عربي بالكامل، اتجاه RTL.
- **المنصة:** صفحة HTML واحدة تعمل كتطبيق PWA (قابل للتثبيت، يعمل بدون إنترنت).
- **بدون خادم / بدون تسجيل دخول:** كل البيانات تُحفظ محلياً في `localStorage`.

---

## 2. التقنيات والبنية

- **ملف واحد** `index.html` يحتوي كل شيء: HTML + CSS داخلي (`<style>`) + JavaScript خام (بدون أي مكتبات أو أطر عمل).
- **Service Worker** `sw.js` للعمل دون اتصال (Cache-first للأصول، Network-first للتنقّل) وإطلاق الإشعارات.
- **`manifest.webmanifest`** لتعريف الـ PWA (الاسم، الأيقونات، `display: standalone`، ألوان الثيم).
- **مجلد `icons/`** أيقونات (192, 512, maskable, apple-touch).
- **مجلد `scripts/`** سكربتات Node لتوليد الأيقونات والشعار (اختيارية، ليست جزءاً من وقت التشغيل).
- **الاستضافة:** ملفات ثابتة (GitHub Pages). يوجد `.nojekyll`.
- **لا يوجد Build step** — HTML/CSS/JS مباشرة.

### هوية التصميم (الثيم)
- خلفية داكنة خضراء (طابع إسلامي): تدرّجات `#0b1f17` → `#0f2a1e` مع توهّج ذهبي.
- لون أساسي ذهبي `--gold:#e9c46a`، أخضر `--green:#16a34a`.
- بطاقات بحواف دائرية كبيرة (18–22px)، ظلال ناعمة، حدود `--line:#1e4535`.
- خط النظام (`-apple-system, Segoe UI, Tahoma`).
- كل المتغيرات في `:root` (انظر قائمة الألوان في الكود).

---

## 3. التنقّل (3 صفحات + شريط سفلي)

شريط تنقّل سفلي ثابت (`nav`) بثلاثة أزرار:
1. **🕌 اليوم** (`page-today`) — التسجيل اليومي.
2. **📊 الملخص** (`page-summary`) — الإحصائيات والتحليلات.
3. **🏆 الإنجازات** (`page-ach`) — الإنجازات + الإعدادات + الأدوات.

التبديل عبر `switchPage(name)` يضيف/يزيل class `active`.

---

## 4. نموذج البيانات (Data Model)

### الصلوات
```
PRAYERS = [fajr🌄, dhuhr☀️, asr🌤️, maghrib🌇, isha🌙]  // id, name, icon
```

### مستويات التسجيل
**وضع الرجال (5 مستويات)** `LEVELS_STD` — لكل مستوى: `id, short, label, color, pts (0-100), d20 (نقاط عدّاد اليوم), emoji, face`:
| المستوى | pts | d20 | اللون |
|---|---|---|---|
| 🌟 قبل الإقامة (green) | 100 | 20 | أخضر |
| 🙂 الركعة الأولى (yellow) | 80 | 15 | أصفر |
| 😐 في المسجد (gray) | 60 | 10 | رمادي |
| 😕 في البيت (oud) | 30 | 5 | بني |
| 😞 فاتتني (red) | 0 | 0 | أحمر |

**وضع النساء (3 مستويات)** `LEVELS_FEMALE`: بين الأذان والإقامة (100/20) · في الوقت (70/10) · فاتتني (0/0). يُفعَّل من الإعدادات (`DB.female`).

### التخزين (`localStorage`, المفتاح `salah_tracker_v1`)
```js
DB = {
  days: { "YYYY-MM-DD": { fajr:'green', dhuhr:'yellow', ..., bonus:{ fajr:{saff:true, walk:true} } } },
  ach: { achId: true },              // الإنجازات المفتوحة
  notes: { "YYYY-MM-DD": { fajr:"نص الملاحظة" } },
  frozenDays: { "YYYY-MM-DD": true }, // الأيام المحميّة بالتجميد
  city: 'jeddah',
  freezes: 1,                         // عدد تجميدات حماية السلسلة (حد أقصى 3)
  freezeMilestone: 0,                 // آخر إنجاز 7 أيام مُكافأ عليه
  female: false                       // وضع النساء
}
```
دوال: `load()` (بقيم افتراضية)، `save()`.

---

## 5. نظام النقاط والرُّتب والسلاسل

### عدّاد اليوم (من 100)
- كل صلاة تعطي `d20` نقطة (حتى 20)، فالمجموع الأقصى الأساسي = 100 (`DAY_MAX`).
- **مكافآت إضافية (+5 لكل عمل)** `BONUS_DEFS`: 🕌 الصف الأول (saff)، 🚶 المشي للمسجد (walk) — للرجال؛ 🌟 سنة راتبة/نافلة (sunnah) — تظهر للنساء. تُفعَّل فقط بعد تسجيل الصلاة. `dayD20(key)` يجمع نقاط الصلوات + المكافآت.
- حلقة تقدّم دائرية (SVG) + رقم متحرّك. عند تجاوز 100 → حالة «أداء استثنائي» (توهّج ذهبي + نبض).

### النقاط الكلية والرُّتب
- **النقطة الكلية = عدد الأيام المثالية** (`isPerfectDay` = كل الصلوات d20 مجموعها ≥ 100، أي كلها قبل الإقامة). `totalPoints()`.
- **الرُّتب** `RANKS`: مبتدئ🌱(0) → مجتهد🌿(3) → محافظ🕌(10) → مُتقن⭐(25) → قدوة🌟(50) → نجم الصف الأول👑(100). مع شريط XP للرتبة التالية.

### السلسلة (Streak) + التجميد (Freeze)
- **يوم نظيف** (`isCleanDay`) = كل الصلوات مسجّلة ولا فيها «فاتتني».
- `recomputeStreak()` يحسب السلسلة ويطبّق **حماية التجميد**: إذا انكسرت السلسلة بيوم واحد وكان قبله وبعده نظيفَين، يُستهلك freeze ويُعلَّم اليوم في `frozenDays` فتستمر السلسلة.
- تُكتسب تجميدة كل 7 أيام من السلسلة (حد أقصى 3، `MAX_FREEZES`).
- شريحتان: 🔥 السلسلة، 🧊 عدد الحمايات.

---

## 6. صفحة «اليوم» (`page-today`)

بالترتيب:
1. **بطاقة عدّاد اليوم الكبيرة** — حلقة SVG + النقاط/100 + وصف الحالة.
2. **بطاقة الصلاة القادمة** — الاسم، الأيقونة، الوقت، والعدّ التنازلي (`renderNextPrayer`).
3. **متصفّح الأيام** (`day-nav`) — أسهم للتنقّل للأيام السابقة (لا يُسمح بالمستقبل). عند تغيير اليوم يُصفَّر توسيع الصلوات اليدوي.
4. **قائمة الصلوات** (`renderPrayers` → `#prayerList`) — بطاقة لكل صلاة:
   - رأس: أيقونة + اسم + وقت الصلاة + شارة الحالة.
   - **الطيّ التلقائي:** كل صلاة تنطوي تلقائياً بمجرد تسجيل الصلاة التي بعدها (لتقليل الزحمة). يظهر زر ▾ في الرأس لفتح/طيّ الخيارات يدوياً. حالة التوسيع اليدوي في `uiExpand` (تُصفَّر عند تغيّر اليوم). آخر صلاة (العشاء) لا تنطوي.
   - جسم البطاقة (يُخفى عند الطيّ): شبكة المستويات (اضغط لتسجيل)، صف ملاحظة، صفوف المكافآت (+5)، زر «📿 أذكار ما بعد الصلاة».
   - إذا دخل وقتها ولم تُسجّل → class `due` (نبض) + شارة «⏰ دخل وقتها».
5. **بطاقة أوقات الصلاة** لليوم + الشروق (`renderTimes`) مع تمييز الوقت الحالي، وزر «📍 تغيير المدينة».

### التفاعلات المهمة
- `setLevel(pid, lid)`: يسجّل/يلغي المستوى، يحفظ، يعيد الرسم، يطلق **رسالة تحفيزية (toast)**، **اهتزازاً خفيفاً**، وحركة **الرقم الطائر «+X»** المتّجهة نحو العدّاد (بطيئة وواضحة لتُقرأ)، ثم يحدّث العدّاد. عند اكتمال اليوم → `celebrateDay`.
- **الرقم الطائر** (`flyPoints`): عنصر `.fly-pts` كبير ذهبي، حركة ~2.2 ثانية: يظهر كبيراً وثابتاً ليُقرأ ثم يسير للحلقة. أثناءها يُمنع تحديث العدّاد (`_flyingDaily`) حتى تصل الحركة.

---

## 7. صفحة «الملخص» (`page-summary`) — `renderSummary`

- **بطاقة الرتبة (Hero):** الرتبة الحالية، شريط XP، شريحتا السلسلة والحماية.
- **صندوق تقييم أداء** (`evalPerf`) — إيموجي + رسالة حسب النسبة المئوية.
- **تبويبات المدى:** اليوم / الأسبوع / الشهر (`curRange`).
- **4 بطاقات إحصائية:** نسبة الأداء، صلوات قبل الإقامة، صلوات فائتة، صلوات مسجّلة.
- **توزيع الصلوات** (`breakdown`) — أشرطة لكل مستوى.
- **مقارنة الأسبوع** بهذا مقابل الماضي (`renderCompare`) — 📈/📉.
- **مخطّط أعمدة** لآخر 14 يوم (`renderChart`, SVG مرسوم يدوياً).
- **رؤية ذكية** (`renderInsight`): يحلّل آخر 30 يوماً → أقوى صلاة، أضعف صلاة + نصيحة مخصّصة (`PRAYER_TIPS`)، واتجاه الأداء أسبوعياً.
- **خريطة حرارية** (`renderHeatmap`) — شبكة ألوان (صلاة × يوم) لآخر 14 يوم.
- **السجل التاريخي** (`renderHistory`) — آخر 14 يوماً بنقاط ملوّنة ونسبة.

---

## 8. صفحة «الإنجازات» (`page-ach`)

- **الإنجازات** (`ACHIEVEMENTS`, `renderAch`) — شبكة بطاقات مقفلة/مفتوحة. `checkAchievements()` يفحص بعد كل تسجيل:
  - أول يوم، يوم مثالي، سلسلة 3/7/30، 10 صلوات صف أول، أسبوع بلا فوات، 30 يوماً مثالياً.
- **الإعدادات:**
  - 👩 مفتاح **وضع النساء** (يبدّل بين 5 و3 مستويات).
  - 🔔 قسم **التذكير والإشعارات** (زر تفعيل الإشعارات + زر التثبيت + ملاحظة توضيحية). *(ملاحظة: هذه الأزرار في الإعدادات وليست في الصفحة الرئيسية.)*
- **الأدوات:**
  - 📤 مشاركة بطاقة إنجاز (صورة PNG مرسومة بـ Canvas عبر `shareCard`، تستخدم Web Share API إن توفّر).
  - 📅 **تذكيرات التقويم** (`.ics`) — 60 يوماً بأوقات مدينتك، تذكير بعد كل صلاة. *(الطريقة المضمونة للتذكير حتى والتطبيق مغلق.)*
  - 💾 تصدير / 📥 استيراد نسخة احتياطية (JSON).
  - 🗑️ حذف كل البيانات.

---

## 9. محرّك أوقات الصلاة (بدون إنترنت)

- حساب فلكي محلي بالكامل (خوارزمية أم القرى): `FAJR_ANGLE=18.5`, عشاء = مغرب + 90 دقيقة (`ISHA_MIN`).
- كود `PT` (IIFE) يحسب: fajr, sunrise, dhuhr, asr, maghrib, isha عبر التاريخ اليولياني وموضع الشمس.
- **قائمة مدن** `CITIES` (خط العرض/الطول + المنطقة الزمنية IANA) — مدن الخليج والعالم العربي وبعض المدن العالمية.
- `tzOffsetHours()` يحسب إزاحة المنطقة الزمنية مع مراعاة **التوقيت الصيفي** تلقائياً عبر `Intl.DateTimeFormat`.
- منتقي مدينة عبر Modal (`cityModal`) مع بحث.
- خطوط العرض العالية: قاعدة سُبع الليل احتياطاً عند تعذّر حساب الفجر.

---

## 10. أذكار ما بعد الصلاة

- قائمة `ADHKAR` (استغفار، تسبيح 33، تحميد 33، تكبير 33، آية الكرسي، المعوّذات...) لكل ذِكر عدد `c`.
- Modal (`adhkarModal`) بعدّاد لكل ذِكر: اضغط للعدّ، اهتزاز خفيف، علامة ✓ عند الإتمام، وزر تصفير. رسالة عند إتمام الكل.

---

## 11. الإشعارات والتذكير

- زر تفعيل يطلب `Notification.requestPermission()`. الحالة تُعرض في نص الزر (`updateNotifBtn`).
- **فحص دوري** (`reminderCheck` كل 30 ثانية): إذا دخل وقت صلاة ولم تُسجَّل خلال 40 دقيقة → تذكير (`fireReminder`) مرة واحدة لكل صلاة يومياً (`notified`).
- الإطلاق يُفضّل عبر **Service Worker** (`registration.showNotification`) مع أيقونة واهتزاز، مع سقوط احتياطي إلى `new Notification`.
- **جدولة مسبقة** (`scheduleReminders`) لصلوات اليوم والغد عبر **Notification Triggers API** (`TimestampTrigger`) لتظهر حتى مع إغلاق التطبيق **على المتصفحات الداعمة فقط** (أندرويد/كروم؛ غير مدعوم على iOS).
- **الضغط على الإشعار** يفتح/يركّز التطبيق (`notificationclick` في `sw.js`).

> **قيود صريحة يجب توثيقها:** (1) لا يمكن ضمان الإشعارات أثناء إغلاق التطبيق تماماً في تطبيق ثابت بدون خادم Push — الحل المضمون هو **تذكيرات التقويم `.ics`**. (2) **الاهتزاز غير مدعوم إطلاقاً على متصفح iOS**، ويعمل على أندرويد فقط. تُغلَّف كل استدعاءات الاهتزاز بدالة `buzz(pattern)` آمنة.

---

## 12. PWA والتحديثات

- تسجيل `sw.js`، وعند وصول نسخة جديدة → `skipWaiting` + toast «يوجد تحديث» ثم إعادة تحميل عند `controllerchange`.
- زر تثبيت (Android/Chrome) يظهر عند `beforeinstallprompt` ويُخفى بعد التثبيت.
- **مهم:** ارفع رقم إصدار الكاش `CACHE` في `sw.js` عند كل تعديل على واجهة التطبيق حتى تصل التحديثات للمستخدمين.

---

## 13. تفاصيل تنفيذية متفرّقة

- تواريخ داخلية بصيغة `YYYY-MM-DD` عبر `ymd(date)`. التاريخ الهجري عبر `Intl.DateTimeFormat('ar-SA-u-ca-islamic')`.
- أرقام عربية عبر `toLocaleString('ar-EG')`.
- Toast موحّد (`toast(msg)`) للرسائل التحفيزية.
- كل النصوص عربية ومحلّية داخل الكود.
- منع تحديد النص / تكبير اللمس (تجربة شبيهة بالتطبيقات الأصلية).
- الحفاظ على منطقة الأمان (`env(safe-area-inset-*)`) لأجهزة iPhone.

---

## 14. ملخّص بجملة واحدة لإعادة البناء

> ابنِ تطبيق PWA عربي (RTL، ملف HTML واحد + service worker، بدون مكتبات، ثيم أخضر/ذهبي داكن) لتتبّع الصلوات الخمس بمستويات تسجيل (5 للرجال/3 للنساء)، مع عدّاد يومي من 100 + مكافآت، نقاط ورُتب وسلاسل بحماية تجميد، إنجازات، تحليلات (نسب/مخطط/خريطة حرارية/رؤية ذكية)، أوقات صلاة محسوبة محلياً (أم القرى) لعدة مدن، أذكار ما بعد الصلاة، تذكيرات (إشعارات + تصدير تقويم .ics)، ونسخ احتياطي JSON — كل البيانات في localStorage.
